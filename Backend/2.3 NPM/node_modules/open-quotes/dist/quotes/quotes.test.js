"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const quotes_1 = require("./quotes");
const quotes_types_1 = require("./quotes.types");
test('Get all quotes defined', () => {
    expect((0, quotes_1.getAllQuotes)().length).toBeGreaterThan(0);
});
test('Get random quote', () => {
    const randomQuote = (0, quotes_1.getRandomQuote)();
    expect(randomQuote).toHaveProperty('quote');
    expect(randomQuote).toHaveProperty('categories');
    expect(randomQuote.author).toHaveProperty('name');
    expect(randomQuote.author).toHaveProperty('link');
    expect(randomQuote.author).toHaveProperty('profileImage');
    expect(randomQuote).toHaveProperty('categories');
});
test('Get quotes by category', () => {
    const quotesProgramming = (0, quotes_1.getQuotesByCategory)(quotes_types_1.QuoteCategory.PROGRAMMING);
    const quotesLife = (0, quotes_1.getQuotesByCategory)(quotes_types_1.QuoteCategory.LIFE);
    const quotesProductivity = (0, quotes_1.getQuotesByCategory)(quotes_types_1.QuoteCategory.PRODUCTIVITY);
    const quoteSoftwareArchiteture = (0, quotes_1.getQuotesByCategory)(quotes_types_1.QuoteCategory.SOFTWARE_ARCHITECTURE);
    expect(quotesProgramming.length).toBeGreaterThan(0);
    expect(quotesLife.length).toBeGreaterThan(0);
    expect(quotesProductivity.length).toBeGreaterThan(0);
    expect(quoteSoftwareArchiteture.length).toBeGreaterThan(0);
});
