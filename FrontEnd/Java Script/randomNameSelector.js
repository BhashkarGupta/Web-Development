function whosPaying(names) {

    /******Don't change the code above*******/

    //Write your code here.
    var selection = Math.floor(Math.random() * names.length);
    return (names[selection] + " is going to buy lunch today!")






    /******Don't change the code below*******/
}